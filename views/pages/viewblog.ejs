<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=locals.fetchBlog.title %> | Blogify</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="bg-gray-900 h-screen">
    <%-include('../partials/header')%>
    <% if (locals.fetchBlog) { %>
     <div
      class="max-w-4xl mt-5 mx-auto dark:text-white rounded-lg bg-gray-800 p-5"
    >
      <h1 class="text-5xl mb-8 font-bold"><%= fetchBlog.title %></h1>
      <div>
        <img class="inline-flex me-3 h-7 w-7 rounded-full ring-2 ring-white" src="<%= fetchBlog.createdBy.profileImageUrl %>" alt="User" loading="lazy"/>
        <span class="my-5"><%= fetchBlog.createdBy.username %></span>
      </div>
      <div class="my-5"><svg class="w-5 h-5 text-gray-500 dark:text-gray-400 inline-flex me-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 24 24">
                    <path fill-rule="evenodd" d="M2 12C2 6.477 6.477 2 12 2s10 4.477 10 10-4.477 10-10 10S2 17.523 2 12Zm11-4a1 1 0 1 0-2 0v4a1 1 0 0 0 .293.707l3 3a1 1 0 0 0 1.414-1.414L13 11.586V8Z" clip-rule="evenodd"/>
                </svg><span><%= fetchBlog.createdAt.toDateString() %></span></div>
      <img src="<%= fetchBlog.coverImageUrl %>" class="w-full h-auto" alt="<%= fetchBlog.title %>" title="<%= fetchBlog.title %>" loading="lazy" width="856" height="481"/>
      <p class="mt-8"><%= fetchBlog.body %></p>
      <div class="mt-10">
      <form action="/<%= fetchBlog._id %>" method="post">
            <textarea id="content" class="sm:text-base text-base py-2 px-3 text-white bg-gray-700 sm:rounded-md rounded-sm focus-within:outline-purple-500 focus-within:outline-2 mb-6 block w-3/4" name="content" required><%= typeof content!=='undefined'?content:'' %></textarea>
        <button
              type="submit"
              class="bg-purple-700 hover:bg-purple-600 hover:cursor-pointer text-white font-bold py-2 px-4 rounded w-fit"
            > Comment
            </button>
      </form>
    </div>
    <% if (locals.fetchComments) { %>
      <p class="text-2xl font-medium mt-6 mb-4"><span class="font-bold"><%= fetchComments.length %></span> Comments</p>
      <hr>
      <% fetchComments.forEach(comment => { %>
       <div class="py-5">
        <img class="inline-flex me-3 h-7 w-7 rounded-full ring-2 ring-white" src="<%= comment.createdBy.profileImageUrl %>" alt="User" loading="lazy"/>
        <span class="my-5"><%= comment.createdBy.username %></span>
        <p class="ms-11"><%= comment.content %></p>
      </div>
      <% }) %>
    <% } %>
    </div>
    <% } %>
    <%-include('../partials/footer')%>
  </body>
</html>
